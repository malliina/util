package com.mle.wicket.component

import org.apache.wicket.markup.html.WebMarkupContainer

/**
 * @author Mle
 */

class AjaxMarkup(id: String) extends WebMarkupContainer(id) {
  setOutputMarkupId(true)
}